import React from "react";

const Navbar = () => {
  return (
    <header className=" sticky top-0  flex justify-center items-center w-screen">
      <nav className="flex  max-w-6xl mx-auto rounded-md border w-full backdrop-blur-xl ">
        Navbar
      </nav>
    </header>
  );
};

export default Navbar;
